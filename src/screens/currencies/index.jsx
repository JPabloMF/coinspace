import React from 'react';
import PropTypes from 'prop-types';

const Curriencies = () => {
  return <div>Curriencies</div>;
};

Curriencies.propTypes = {
  props: PropTypes.object.isRequired
};

export default Curriencies;
