import React from 'react';
import PropTypes from 'prop-types';

const MarketTrends = () => {
  return <div>MarketTrends</div>;
};

MarketTrends.propTypes = {
  props: PropTypes.object.isRequired
};

export default MarketTrends;
